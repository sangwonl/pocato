{
  "name": "pocato",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/react",
    "build:example": "npm run build && npm run build -w packages/example",
    "dev": "npm run dev -w packages/example"
  }
}
