{
  "name": "pocato",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/react && npm run build -w packages/example",
    "dev": "npm run dev -w packages/core & npm run dev -w packages/react & npm run dev -w packages/example"
  }
}
